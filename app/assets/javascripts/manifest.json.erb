<% icon_sizes = Rails.configuration.serviceworker.icon_sizes %>
{
  "name": "My Progressive Rails App",
  "short_name": "Progressive",
  "start_url": "/",
  "icons": [
  <% icon_sizes.map { |s| "#{s}x#{s}" }.each.with_index do |dim, i| %>
    {
      "src": "<%= image_path "serviceworker-rails/heart-#{dim}.png" %>",
      "sizes": "<%= dim %>",
      "type": "image/png"
    }<%= i == (icon_sizes.length - 1) ? '' : ',' %>
  <% end %>
  ],
  "theme_color": "#000000",
  "background_color": "#FFFFFF",
  "display": "fullscreen",
  "orientation": "portrait"
}

{
  "name": "moguta", #アプリの長い名前
  "short_name": "moguta", #アプリの短い名前
  "start_url": "/", #起動時のパス
  "icons": [
    {
      "src": "<%=image_path("moguta.png")%>", #アイコン
      "type": "image/png",
      "sizes": "192x192"
    }
  ],
  "theme_color": "#ffd753", #テーマ
  "background_color": "#ffd753", #起動時の色
  "display": "fullscreen",
  "orientation": "portrait"
}
